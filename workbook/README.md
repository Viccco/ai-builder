# Workbook

Your outputs land here: exported labels, the taxonomy, judge prompts, scoring results, the failure log, the monitoring plan, the eval plan.

The apps download files to wherever your browser puts downloads. Move them here and tell Claude Code the path.

Nothing in this folder is committed by default (see `.gitignore`), so your own work stays yours.
